[tool.poetry]
name = "dataservice"
version = "0.1.0"
description = "Python lightweight scraping framework"
authors = ["lucaromagnoli <romagnoli.luca@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
bs4 = "^0.0.2"
html5lib = "^1.1"
httpx = "^0.27.0"
pydantic = "^2.8.2"


[tool.poetry.group.dev.dependencies]
black = "^23.1.0"
mypy = "^1.0.0"
pytest = "^7.2.1"
isort = "^5.12.0"
pytest-mock = "^3.14.0"
pytest-asyncio = "^0.23.7"
pytest-httpx = "^0.30.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
